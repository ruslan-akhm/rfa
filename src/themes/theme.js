import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  typography: {
    fontFamily: '"Roboto"',
    fontSize: 14,
  },
  palette: {
    primary: { main: "#0346F2" },
    secondary: { main: "#F2F5FE" },
  },
});
